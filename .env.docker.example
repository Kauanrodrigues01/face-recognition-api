# Example environment file for Docker deployment
# Copy this file to .env.docker and customize the values

# Application Port
APP_PORT=8000

# GPU Support - Set to 'true' if running on a machine with NVIDIA GPU
# This will install onnxruntime-gpu instead of onnxruntime
USE_GPU=false

# Project Configuration
PROJECT_NAME=Face Recognition API
VERSION=0.1.0
DESCRIPTION=API for face recognition with PostgreSQL backend
API_V1_STR=/api/v1

# Database Configuration
# For database running on host machine, use host.docker.internal
DATABASE_URL=postgresql+asyncpg://postgres:password@host.docker.internal:5432/face_recognition
DATABASE_TEST_URL=postgresql+asyncpg://postgres:password@host.docker.internal:5433/face_recognition_test

# Security - IMPORTANT: Generate new keys for production!
# Generate SECRET_KEY with: openssl rand -hex 32
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Face Recognition Encryption - IMPORTANT: Generate new key for production!
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FACE_ENCRYPTION_KEY=YgEeuOA6sQMntCX_pvHRfEBdhyyezbBuqEmJgzvHb0w=

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000